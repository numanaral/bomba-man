import { string } from 'prop-types';

const propTypes = {
	{{#each _props as |prop|}}
	{{prop}}: string,
	{{/each}}
};

const defaultProps = {
	{{#each _props as |prop|}}
	{{prop}}: 'default {{prop}} value',
	{{/each}}
};

export { propTypes, defaultProps };
  