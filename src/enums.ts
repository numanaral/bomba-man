enum Direction {
	UP = 'UP',
	RIGHT = 'RIGHT',
	DOWN = 'DOWN',
	LEFT = 'LEFT',
}

enum Axis {
	X = 'X',
	Y = 'Y',
}

enum ExplosionState {
	Firing = 'Firing',
	Exploding = 'Exploding',
	Exploded = 'Exploded',
}

export { Direction, Axis, ExplosionState };
